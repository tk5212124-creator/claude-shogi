# 回帰テスト

`shogi.html` は1枚のHTMLなので、テストは Playwright でページを開いて中の関数を直接叩く形にしてある。

```
node tests/repetition.mjs        [new|old] [引き継ぐ手数] [試行回数]
node tests/replay-fidelity.mjs
node tests/advisor-palace.mjs    [new|old]
node tests/palace-is-diag-only.mjs
node tests/go-stone-ownership.mjs [new|old]
node tests/go-rules.mjs
node tests/cross-layer-capture.mjs
node tests/mixed-go-capture.mjs
node tests/shogi-pieces-cannot-surround.mjs
node tests/fx-layout.mjs        [new|old]
node tests/bgm.mjs
node tests/board-text-fit.mjs  [new|old]
node tests/score-modes.mjs
node tests/theme.mjs
node tests/check-points.mjs
node tests/piece-value.mjs
node tests/score-and-territory.mjs
```

`old` を指定する場合は、比較したい版のHTMLを `OLD_HTML` 環境変数で渡す
（例：`OLD_HTML=/path/to/old.html node tests/repetition.mjs old 48 6`）。

## case273-repetition.json

CPU同士・超高速で起きた **273手・捕獲0・成り0** の実局。
先手が49手目から最後まで、**112回連続で飛車 (7,3)⇄(7,4) を往復**し続けている。

盤は素の本将棋（`pts` は全て null、`diag` も全て null、`palace=false`）。
`banSennichite=true` / `sennichiteMode="avoid"` でも止まらないのは、相手が別の駒を
少しずつ動かすため局面が完全一致せず、局面単位の千日手回避が働かないから。

### repetition.mjs が見るもの

この棋譜どおりに指定手数まで進めてから、あとをCPUに任せて160手指させ、
**同じ駒が直前の自分の手を完全に逆戻りした連続回数**を数える。

48手目から引き継いだ結果（各6回）:

| | 旧（この問題が出た版） | 現在 |
|---|---|---|
| 先手の最大連続逆戻り 平均／最悪 | 12.8 ／ 19 | **0 ／ 0** |
| 後手の最大連続逆戻り 平均／最悪 | 7.3 ／ 14 | **0 ／ 0** |
| 160手のあいだの捕獲数 | 0 | 16.8 |
| 平均nodes | — | 23 |

120手目・200手目・272手目から引き継いでも同じ傾向。

### replay-fidelity.mjs が見るもの

同じ棋譜273手を最初から再生し、報告された最終盤と1マスも違わないこと。
着手の適用そのものが正しいかの確認。

## advisor-palace.mjs が見るもの

仕・士（斜めにしか動かない宮の駒）は「宮に引かれた斜め線の上」しか動けない。
ところが「宮かどうか」の判定は *線に接するマスがあるか* という広い条件なので、
そのままだと線の通っていない場所へ行けても打ててもしまい、そこから二度と動けなくなる。

×（交差）を含む3×3の宮を作り、全ての交点・全てのマスに士を置いて総当たりする。

| | 直す前 | 現在 |
|---|---|---|
| 線に沿っていない手 | 0 | 0 |
| 行った先で動けなくなる手 | **4** | **0** |
| 打てる点のうち打つと動けなくなる点 | **16 / 32** | **0 / 16** |
| マスに打つと動けなくなる数 | **8** | **0** |

「線に沿っていない手」は元から0。報告にあった「線以外の方向へ動いている」ように見えるのは、
線の無い場所へ打たれた駒がそこに立ち続けている結果だった。

## palace-is-diag-only.mjs が見るもの

九宮は「宮マス（斜め線）」だけで表す。盤の位置から宮を決める常駐の仕組みは持たない。

以前は `RULES.palace` というフラグがあり、「適用」「作成」を押すたびに宮マスを
全部消して標準の宮を描き直していた。そのため**手で描いた宮マスが黙って消えて**いた。

| | 直す前 | 現在 |
|---|---|---|
| 手描きの宮マス → 「作成」で盤を広げる | 2マス → **0マス** | 2マス → **2マス** |
| 手描きの宮マス → 「適用」 | 1マス → **0マス** | （プリセットは盤ごと作り直すので0で正しい） |
| シャンチー／チャンギ適用後の宮マス | 8マス | 8マス |
| 「九宮をつくる」ボタン | チェック＋適用が必要 | 押した時点で描かれる |
| 古い保存データ（`palace:true` 入り） | — | 読み込める。`RULES` には残らない |

## go-stone-ownership.mjs が見るもの

碁石は **黒＝先手の駒／白＝後手の駒** で、色そのものが持ち主を決める。
誰が打っても黒石は先手の石、白石は後手の石になる。
そのため **先手は盤上の黒石を、後手は白石を取れない**（自分の石だから）。

色と持ち主が食い違うとこの前提が崩れる。駒の種類が変わりうる経路を全部検査する。

| | 直す前 | 現在 |
|---|---|---|
| オセロで裏返された碁石 | 黒石なのに後手のもの（**食い違い**） | 白石＝後手（一致） |
| 読み（simulate）の中でも同じ | **食い違い** | 一致 |
| ランダム置換で碁石になったとき | 一致 | 一致 |
| 編集で「後手の黒石」を置く | 後手のまま（**食い違い**） | 先手に直る |
| 黒石を黒で囲む | 取れない（正しい） | 取れない（正しい） |

裏返しは**色ごと**変える（黒石が後手のものになるのではなく、白石になる）。

## go-rules.mjs が見るもの

囲んで取れるのは碁石だけで、取られる方は駒の種類を問わない。
上下左右でつながった同じ側の駒を1つの群とし、群の呼吸点が0になったら丸ごと同時に取られる。
自殺手は打てないが、その手で相手の群を取って呼吸点が生まれるなら合法。

| 見るもの | 期待 |
|---|---|
| 囲碁プリセットの初期持ち駒 | 黒400個・白400個 |
| 囲碁プリセットの `allowPass` | true |
| 双方が続けてパス | 終局する（決着のつけ方は手詰まりと同じ扱い） |
| 間に通常の着手が入ってからパス | 数え直しになり終局しない |
| 3子を完全に囲む | 3子まとめて取れ、相手の持ち駒に3個入る |
| 呼吸点が1つでも残っている群 | 取られない |
| 自殺手 | 打ち先の候補に入らない／人も指せない／CPUも生成しない |
| 相手を取って生きる手 | 候補に入り、指せて、相手の駒が取れる |
| 読み(simulate)と実盤 | 盤・持ち駒とも食い違い0 |

仕上げに実際に120手打たせて
「読みと実盤の食い違い0／自殺手0／呼吸点0のまま残った群0」も確かめる。

## cross-layer-capture.mjs が見るもの

囲んで取れるのは**碁石だけ**。取られる方は駒の種類を問わない。
呼吸点を塞げるのは ①隣の相手の碁石 ②辺を挟む別の層の相手の碁石2つ
③碁石をマスに置けるルールのときだけ、空き交点も「2辺が碁石で塞がり」なら塞がり ④盤の外、の4つ。

| 見るもの | 期待 |
|---|---|
| 交点の碁石を敵の歩4枚で囲む／同じ形を敵の碁石4個で囲む | 生き／呼吸点0 |
| 玉将を4頂点の碁石で囲む／同じ形を敵の兵4枚で囲む | 呼吸点0／生き |
| 歩を敵の歩4枚で囲む | 生き |
| 囲まれたマスの駒の隣に、呼吸点のある味方 | 群は1つのまま＝**囲まれた駒だけ取られる**（味方は残る） |
| 辺を挟む碁石1マス／2マス | 通れる／塞がり |
| 同じ2マスを味方の側から見る／盤の外を含む辺 | 通れる |
| 盤の縁の交点を隣のマス2つで挟む | 生き・縁に置くのも自殺手にならない |
| 空き交点の2辺が碁石で塞がり（碁石をマスに置けるルール） | 塞がり |
| 交点だけのルール | ③は使わない |
| 10変種の初期配置（本将棋〜大局将棋・囲碁） | 取られる駒は1つも無い（誤爆しない） |

## mixed-go-capture.mjs / case26-mixed-go.json が見るもの

碁石を持ち駒に入れた本将棋（`pointMode:"auto"`／`captureAll:true`）で報告された26手の実局。
古い版では次の4つが起きていた。

| 見るもの | 古い版 | 現在 |
|---|---|---|
| 飛車がマスへ動いただけで交点の碁石が取れる（12手目・16手目） | **取れた** | **0個** |
| 隣に空き交点があるのに取られた黒石（pt[1,3] / pt[1,4]） | **消えた** | **残る** |
| 白石8個で囲まれた最下段の金・玉・金 | **残った** | **取れる**（20/22/26手目） |
| 囲みで取った駒が棋譜に出るか | 出ない | `取:玉将` `取:碁黒・碁黒` として出る |
| 囲まれていない隣の銀将 | 残る | 残る |
| 26手を通して「取られるはずなのに残っている駒」 | — | 0個 |

## piece-value.mjs が見るもの

駒価は駒ごとの固定値ではなく、駒の動きと置かれる層から共通の式で出す。

    王駒      V = 20
    それ以外  V = 1 + I + S + Q×(2R + C) + 2N + L      Q = (I===1) ? 2 : 1

基準値32駒と一致することを確認する。

| | 期待 |
|---|---|
| チェス | P1 N3 B5 R5 Q9 K20 |
| 本将棋 | 歩香桂銀金1・角5・飛5・玉王20 |
| チャンギ | 兵2 士3 象3 馬5 包7 車13 漢楚20 |
| 囲碁石・オセロ駒 | 1 |

## score-and-territory.mjs が見るもの

盤面の点数は **ΣV(盤上の駒) + G(囲碁の領域点)**。持将棋点はこれに取った駒の駒価を足す。

| 見るもの | 期待 |
|---|---|
| 本将棋の盤上（飛角＋歩9） | 19点（王は盤上集計から除外） |
| 持ち駒に飛車と王 | 25点（取った王の20点もここに入る） |
| 「取った駒を点数に足す」をOFF | 0点（盤上だけ数える） |
| 黒地9・白地4 の碁盤 | 黒 石7+地9=16、白 石5+地4=9 |
| 碁石が無い盤 | 領域点0 |
| 王手で加点（ルールON） | 王手1回で1点 |
| 双方入玉でないとき | 点数の多い方の勝ち |
| 持将棋＝引き分け設定 | 点数を見ずに引き分け |

## shogi-pieces-cannot-surround.mjs / case12-surround-drop.json が見るもの

碁石を持ち駒に入れた本将棋で、**打った歩が次の手で消えた**と報告された12手の実局。
先手の歩 (3行2列) は上下左右すべてが敵の**将棋駒**（銀・歩・歩・歩）だが、
囲めるのは碁石だけなので取られてはいけない。

| 見るもの | 期待 |
|---|---|
| 先手の歩 (3行2列) | 上下左右が敵の将棋駒でも**残る** |
| 12手目の棋譜 | `△歩兵 打 (4行2列)`（`取:歩兵` は付かない） |
| 白石4個で囲まれた後手の金将 (9行4列) | 取れる（8手目 `取:金将`） |
| 敵の歩4枚に囲まれた歩／敵の碁石4個に囲まれた歩 | 生き／取れる |

## fx-layout.mjs が見るもの

演出のあいだに画面が崩れないこと。旧版は `body` そのものを揺らしていたため、
`body::before`（背景）が body 基準の巨大な要素になり、**文書の横幅が 390→488px に広がって**
iOS が画面を縮めて表示していた（見出しがステータスバーに重なる）。
揺れは `#page` に掛けるようにし、`html`/`body` に `overflow-x:clip` を付けた。

図鑑ボタンも盤に重ねる置き方をやめ、盤の下の行（通常の並び）に移した。

| 見るもの | 旧版 | 現在 |
|---|---|---|
| 演出の最中の文書の横幅（画面390px） | **488px（はみ出し）** | **390px** |
| 演出の前後の盤幅・見出しの位置 | 変わらない | 変わらない |
| 図鑑ボタンが盤の駒に重なるか | 重ならない | 重ならない |
| 図鑑ボタンがルール設定より手前に出るか | **出る** | **出ない** |
| pageerror（390px・320px） | 0 | 0 |

## check-points.mjs が見るもの

4人チェスの王手加点。royal が複数あると1手で同時に王手を掛けられる。

| 1手で同時に王手を掛けた数 | クイーン系 | それ以外 |
|---|---|---|
| 1つ | 1点 | 1点 |
| 2つ | 1点 | 5点 |
| 3つ以上 | 5点 | 20点 |

| 見るもの | 期待 |
|---|---|
| クイーン系か（クイーン・奔王／飛車・歩兵・包） | ○○／××× |
| 単発の王手（クイーン／飛車） | 1点／1点 |
| ダブルチェック（クイーン／飛車） | 1点／5点 |
| トリプルチェック（クイーン／飛車） | 5点／20点 |
| 王手が掛かっていない手 | 0点 |
| 「王手で加点」ON/OFF | 点数の差がそのまま出る |

## bgm.mjs が見るもの

`bgm/` の mp3 を `<audio>` で流す。効果音（Web Audio）とは別系統。

| 見るもの | 期待 |
|---|---|
| 曲の一覧と選択肢 | 13曲（Battle Loop 系2・Kuro Kesa Pulse 系2・Kurogane Pulse・Crimson Shamisen Clash 系2・鬼門の鼓動・低音の襲撃 系2・進め モンスターバトル 系2・Mudai） |
| BGMをONにする前 | mp3リクエスト0件・`<audio>` も作られない（`preload="none"`） |
| ONにした後 | 再生が始まる・音量0.42・読み込むのは選んだ1曲だけ |
| 曲を選び直す | その場で切り替わり、再生は続く |
| くり返し方のボタン | 🔁全曲 → 🔂1曲 → 🔀ランダム → 🔁全曲 と回る |
| ランダムの選び方 | 200回試して「いまと同じ曲」を選んだ回数0・残り12曲すべてが選ばれる |
| 曲が終わったら（全曲） | 次の曲へ進み、選択欄の表示も追随する |
| 「音・演出」をOFF | BGMも止まる |
| BGMボタンでON→OFF | 鳴る／止まる |
| 全曲の長さ | 2分43秒〜4分55秒（読み込めないファイル0） |
| pageerror | 0 |

## board-text-fit.mjs が見るもの

駒の字の大きさが「1マスの大きさ」に比例しているか。
縦書きの span は箱の高さが 0 に潰れてブラウザごとに描画が違うので、
実際の描画ではなく **文字の大きさ ÷ マスの大きさ** で判定する。

| 盤 | 旧版 はみ出す駒 | 現在 |
|---|---|---|
| 本将棋 9×9 | 0 | 0 |
| 中将棋 12×12 | **92（全部・縦1.10倍）** | **0** |
| 大将棋 15×15 | 0 | 0 |
| 摩訶大大将棋 19×19 | 0 | 0 |
| 大局将棋 36×36 | **804（全部・横2.06倍）** | **0** |
| シャンチー 9×8 | 0 | 0 |

390px・320px の両方で確認する。

## score-modes.mjs が見るもの

持将棋の決め方（`jishogiMode`）と、いつも出す点数。

| 見るもの | 期待 |
|---|---|
| 点数の表示（点数計算／点数だけ／引き分け設定） | 出る／出る／出ない |
| 点数計算の中身 | 盤上＋取った駒（飛5＋取った角5＝10点） |
| 点数だけの中身 | 盤上だけ（飛5＝5点） |
| 点数だけのモードで玉を取る | **終局しない** |
| 点数だけのモードで相手の盤上を0にする | 終局・勝ち |
| 同じ形で「点数計算で勝敗を決める」なら玉を取った時点 | 終局 |
| 王手の加点 | 点数計算には入る／点数だけには入らない |

## theme.mjs が見るもの

盤・自分の駒・相手の駒・背景の色を別々に選べる。

| 見るもの | 期待 |
|---|---|
| 選択肢 | 4つとも6種 |
| 選ぶと CSS 変数と実際の描画が変わる | 盤の背景・駒の文字色・body の背景とも変わる |
| 覚えるか | `localStorage` の `shogiTheme` に入る |
| 読み直したあと | 色も選択欄の表示も残る |
| 「色を元に戻す」 | 既定色に戻る |
| プリセットを変えても | 色は残る |
